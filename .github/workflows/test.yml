name: Test

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  call-pixi-tasks-workflow:
    uses: ASFHyP3/actions/.github/workflows/reusable-pixi-tasks.yml@support-pixi
    with:
      environments: >-
        ["py310", "py311", "py312"]
      tasks: >-
        ["tests"]
    permissions:
      contents: read
